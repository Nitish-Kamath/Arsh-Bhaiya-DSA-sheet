https://leetcode.com/problems/find-all-duplicates-in-an-array/

Solution using cpp
[time complexity O(n) and space complexityO(n) ]


class Solution {
public:
    vector<int> findDuplicates(vector<int>& nums) {

    map<int, int> mp;        
    vector<int> retnums;
    for (int i = 0; i < nums.size(); i++)
    {
        mp[nums[i]]++;
        if (mp[nums[i]] == 2)
            retnums.push_back(nums[i]);
    }
        return retnums;
    }
};
